claimProtocolRevenues.t.sol
├── when the caller is not the admin
│  └── it should revert
└── when the caller is the admin
   ├── given the protocol revenues are zero
   │  └── it should revert
   └── given the protocol revenues are not zero
      ├── it should claim the protocol revenues
      ├── it should update the protocol revenues
      └── it should emit a {ClaimProtocolRevenues} event
