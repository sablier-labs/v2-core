WithdrawMultiple_Integration_Concrete_Test
├── when delegate call
│  └── it should revert
└── when no delegate call
   ├── when arrays have unequal length
   │  └── it should revert
   └── when arrays have equal length
      ├── when array length is zero
      │  └── it should do nothing
      └── when array length is not zero
         ├── given all stream IDs reference to null
         │  └── it should revert
         ├── given some stream IDs reference to null
         │  └── it should revert
         └── given no stream IDs reference to null
            ├── given all streams with "DEPLETED" status
            │  └── it should revert
            ├── given some streams with "DEPLETED" status
            │  └── it should revert
            └── given no streams with "DEPLETED" status
               ├── when some amounts are zero
               │  └── it should revert
               └── when no amounts are zero
                  ├── when some amounts overdraw
                  │  └── it should revert
                  └── when no amounts overdraw
                     ├── it should make the withdrawals
                     ├── it should update the statuses
                     ├── it should update the withdrawn amounts
                     └── it should emit multiple {WithdrawFromLockupStream} events
