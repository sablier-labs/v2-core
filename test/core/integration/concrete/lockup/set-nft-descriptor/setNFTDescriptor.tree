SetNFTDescriptor_Integration_Concrete_Test
├── when caller is not admin
│  └── it should revert
└── when caller is admin
   ├── when new NFT descriptor is same as current NFT descriptor
   │  ├── it should re-set the NFT descriptor
   │  └── it should emit {SetNFTDescriptor} and {BatchMetadataUpdate} events
   └── when new NFT descriptor is not same as current NFT descriptor
      ├── it should set the new NFT descriptor
      └── it should emit {SetNFTDescriptor} and {BatchMetadataUpdate} events
