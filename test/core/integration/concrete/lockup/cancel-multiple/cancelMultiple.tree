CancelMultiple_Integration_Concrete_Test
├── when delegate call
│  └── it should revert
└── when no delegate call
   ├── when array length is zero
   │  └── it should do nothing
   └── when array length is not zero
      ├── given all stream IDs reference to null
      │  └── it should revert
      ├── given some stream IDs reference to null
      │  └── it should revert
      └── given no stream IDs reference to null
         ├── given all streams are cold
         │  └── it should revert
         ├── given some streams are cold
         │  └── it should revert
         └── given all streams are warm
            ├── when caller unauthorized for all streams
            │  └── it should revert
            ├── when caller unauthorized for some streams
            │  └── it should revert
            └── when caller authorized for all streams
               ├── given no stream is cancelable
               │  └── it should revert
               ├── given some streams are cancelable
               │  └── it should revert
               └── given all streams are cancelable
                  ├── it should mark the streams as canceled
                  ├── it should make the streams as non cancelable
                  ├── it should refund the sender
                  ├── it should update the refunded amounts
                  ├── it should not burn the NFT for all streams
                  └── it should emit {CancelLockupStream} events for all streams
