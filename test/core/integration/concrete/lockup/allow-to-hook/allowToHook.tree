AllowToHook_Integration_Concrete_Test
├── when caller not admin
│  └── it should revert
└── when caller admin
   ├── when provided address not contract
   │  └── it should revert
   └── when provided address contract
      ├── when provided address not return interface id
      │  └── it should revert
      └── when provided address returns interface id
         ├── it should put the address on the allowlist
         └── it should emit a {AllowToHook} event
