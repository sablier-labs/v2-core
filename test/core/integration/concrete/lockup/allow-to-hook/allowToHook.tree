AllowToHook_Integration_Concrete_Test
├── when caller not admin
│  └── it should revert
└── when caller admin
   ├── when provided address is eoa
   │  └── it should revert
   └── when provided address is contract
      ├── when provided address has unsupported interface
      │  └── it should revert
      └── when provided address supports interface
         ├── it should put the address on the allowlist
         └── it should emit a {AllowToHook} event
