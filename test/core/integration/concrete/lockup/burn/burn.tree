Burn_Integration_Concrete_Test
├── when delegate call
│  └── it should revert
└── when no delegate call
   ├── given null
   │  └── it should revert
   └── given not null
      ├── given stream has not been depleted
      │  ├── given status is "PENDING"
      │  │  └── it should revert
      │  ├── given status is "STREAMING"
      │  │  └── it should revert
      │  ├── given status is "SETTLED"
      │  │  └── it should revert
      │  └── given status is "CANCELED"
      │     └── it should revert
      └── given stream has been depleted
         ├── when unauthorized caller
         │  └── it should revert
         └── when authorized caller
            ├── given NFT does not exist
            │  └── it should revert
            └── given NFT exists
               ├── given non transferable NFT
               │  ├── it should burn the NFT
               │  └── it should emit a {MetadataUpdate} event
               └── given transferable NFT
                  ├── when caller is approved third party
                  │  ├── it should burn the NFT
                  │  └── it should emit a {MetadataUpdate} event
                  └── when caller is NFT owner
                     ├── it should burn the NFT
                     └── it should emit a {MetadataUpdate} event
