CreateWithTimestamps_LockupTranched_Integration_Concrete_Test
├── when delegate call
│  └── it should revert
└── when no delegate call
   ├── when sender is zero address
   │  └── it should revert
   └── when sender is not zero address
      ├── when recipient is zero address
      │  └── it should revert
      └── when recipient is not zero address
         ├── when deposit amount is zero
         │  └── it should revert
         └── when deposit amount is not zero
            ├── when start time is zero
            │  └── it should revert
            └── when start time is not zero
               ├── when tranche count is zero
               │  └── it should revert
               └── when tranche count is not zero
                  ├── when tranche count is too high
                  │  └── it should revert
                  └── when tranche count is not too high
                     ├── when tranche amounts sum overflows
                     │  └── it should revert
                     └── when tranche amounts sum does not overflow
                        ├── when start time is greater than first tranche timestamp
                        │  └── it should revert
                        ├── when start time is equal to first tranche timestamp
                        │  └── it should revert
                        └── when start time is less than first tranche timestamp
                           ├── when tranche timestamps are not ordered
                           │  └── it should revert
                           └── when tranche timestamps are ordered
                              ├── when deposit amount not equal tranche amounts sum
                              │  └── it should revert
                              └── when the deposit amount equals tranche amounts sum
                                 ├── when broker fee is too high
                                 │  └── it should revert
                                 └── when broker fee is not too high
                                    ├── when asset is not contract
                                    │  └── it should revert
                                    └── when asset is contract
                                       ├── when asset misses ERC20 return value
                                       │  ├── it should create the stream
                                       │  ├── it should bump the next stream ID
                                       │  ├── it should mint the NFT
                                       │  ├── it should emit {CreateLockupTranchedStream} and {MetadataUpdate} events
                                       │  └── it should perform the ERC-20 transfers
                                       └── when asset does not miss ERC20 return value
                                          ├── it should create the stream
                                          ├── it should bump the next stream ID
                                          ├── it should mint the NFT
                                          ├── it should emit {CreateLockupTranchedStream} and {MetadataUpdate} events
                                          └── it should perform the ERC-20 transfers
