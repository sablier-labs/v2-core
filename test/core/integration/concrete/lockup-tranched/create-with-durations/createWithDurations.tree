CreateWithDurations_LockupTranched_Integration_Concrete_Test
├── when delegate call
│  └── it should revert
└── when no delegate call
   ├── when tranche count too high
   │  └── it should revert
   └── when tranche count not too high
       ├── when index one or higher contains zero duration
       │  └── it should revert
       └── when index one or higher not contain zero duration
          ├── when tranche timestamp calculations overflow
          │  ├── when start time is greater than first tranche timestamp
          │  │  └── it should revert
          │  └── when tranche timestamps are not ordered
          │     └── it should revert
          └── when tranche timestamp calculations do not overflow
             ├── it should create the stream
             ├── it should bump the next stream ID
             ├── it should mint the NFT
             ├── it should emit {MetadataUpdate} and {CreateLockupTranchedStream} events
             └── it should perform the ERC-20 transfers
