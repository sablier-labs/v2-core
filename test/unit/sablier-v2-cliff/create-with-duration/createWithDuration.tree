create-with-duration.t.sol
├── when the duration calculation overflows uint256
│  └── it should revert
└── when the duration calculation does not overflow uint256
   ├── when the cliff duration calculation overflows uint256
   │   └── it should revert
   └── when the cliff duration calculation does not overflow uint256
       └── it should create the cliff stream with duration
