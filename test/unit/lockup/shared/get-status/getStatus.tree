getStatus.t.sol
├── when the stream has not been created
│  └── it should return NULL
└── when the stream has been created
   ├── when nothing else happened
   │  └── it should return ACTIVE
   ├── when the stream has been canceled
   │  └── it should return CANCELED
   └── when the stream has been fully withdrawn
      └── it should return DEPLETED
