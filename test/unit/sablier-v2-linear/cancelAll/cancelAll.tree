cancelAll.t.sol
├── when the streamIds array is empty
│  └── it should revert
└── when the streamIds array is not empty
   ├── when the streamIds array has only non existing streams
   │  └── it should revert
   ├── when the streamIds array has only a single non existing stream on the first position
   │  └── it should revert
   ├── when the streamIds array has only a single non existing stream on the last position
   │  └── it should revert
   └── when the streamIds array has only existing streams
      ├── when the caller of all the streams is neither the sender nor the recipient
      │  └── it should revert
      ├── when the caller of the first stream is neither the sender nor the recipient
      │  └── it should revert
      ├── when the caller of the last stream is neither the sender nor the recipient
      │  └── it should revert
      ├── when the caller of all of the streams is the recipient
      │  └── it should cancel multiple streams
      └── when the caller of all of the streams is the sender
         ├── when the streamIds array has only non-cancelable streams
         │  └── it should revert
         ├── when the streamIds array has a single non-cancelable stream on the first position
         │  └── it should revert
         ├── when the streamIds array has a single non-cancelable stream on the last position
         │  └── it should revert
         └── when the streamIds array has only cancelable streams
            ├── when the streamIds array has only ended streams
            │  ├── it cancels all the streams
            │  ├── it deletes all the streams
            │  └── it emits multiple Cancel event
            ├── when the streamIds array has only not ended streams
            │  ├── it cancels all the streams
            │  ├── it deletes all the streams
            │  └── it emits multiple Cancel event
            └── when the streamIds array has ended streams and not ended streams
               ├── it cancels all the streams
               ├── it deletes all the streams
               └── it emits multiple Cancel event

