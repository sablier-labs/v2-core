decreaseAuthorization.t.sol
├── when the calculation underflows uint256
│  └── it should revert
└── when the calculation does not underflow uint256
   ├── when the authorization is decreased entirely
   │  └── it should set the authorization to zero
   └── when the authorization is decreased partially
      ├── it should decrease the authorization
      └── it should emit an Authorize event

